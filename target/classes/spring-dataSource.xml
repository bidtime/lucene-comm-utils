<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:tx="http://www.springframework.org/schema/tx"
	   xmlns:context="http://www.springframework.org/schema/context"
	   xmlns="http://www.springframework.org/schema/beans"
	   xsi:schemaLocation="http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
           http://www.springframework.org/schema/context
           http://www.springframework.org/schema/context/spring-context-3.2.xsd
           http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-3.2.xsd">

	<!-- 引入jdbc配置文件 -->
	<context:property-placeholder location="classpath:data.properties" ignore-unresolvable="true"/>

	<context:annotation-config />
	<context:component-scan base-package="com.utils.lucene.dal.carvin.dao" />

	<!-- carvin注解事务管理器test -->
	<tx:annotation-driven transaction-manager="carvinTxManager"/>

	<!-- carvin数据源,c3p0 -->
	<bean id="carvinDataSource" class="com.mchange.v2.c3p0.ComboPooledDataSource" destroy-method="close">
		<property name="driverClass" value="${carvinDS.driverClass}"/>
		<property name="jdbcUrl" value="${carvinDS.jdbcUrl}"/>
		<property name="user" value="${carvinDS.user}"/>
		<property name="password" value="${carvinDS.password}"/>
		<property name="minPoolSize" value="${carvinDS.minPoolSize}"/>
		<property name="maxPoolSize" value="${carvinDS.maxPoolSize}"/>
		<property name="initialPoolSize" value="${carvinDS.initialPoolSize}"/>
		<property name="maxIdleTime" value="300"/>
		<property name="idleConnectionTestPeriod" value="60"/>
		<property name="acquireRetryAttempts" value="3"/>
		<property name="acquireRetryDelay" value="300"/>
		<property name="maxStatements" value="0"/>
		<property name="maxStatementsPerConnection" value="100"/>
		<property name="numHelperThreads" value="6"/>
		<property name="maxAdministrativeTaskTime" value="5"/>
		<property name="preferredTestQuery" value="SELECT 1"/>
	</bean>

	<!-- carvin事务管理器 -->
	<bean id="carvinTxManager"
		  class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
		<property name="dataSource" ref="carvinDataSource"/>
		<qualifier value="carvin"></qualifier>
	</bean>

	<!-- carvin事务管理模板 用于编程式事务 -->
	<bean id="carvinTxTemplate"
		  class="org.springframework.transaction.support.TransactionTemplate">
		<property name="transactionManager" ref="carvinTxManager"/>
	</bean>

	<!-- 创建SqlSessionFactory，同时指定数据源 -->
	<bean id="carvinSqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
		<property name="dataSource" ref="carvinDataSource"/>
	</bean>

	<!-- Mapper接口所在包名，Spring会自动查找其下的Mapper -->
	<bean id="carvinSqlMapper" class="org.mybatis.spring.mapper.MapperScannerConfigurer">
		<property name="sqlSessionFactoryBeanName" value="carvinSqlSessionFactory"/>
		<property name="basePackage" value="com.utils.lucene.dal.carvin.dao"/>
	</bean>

</beans>
